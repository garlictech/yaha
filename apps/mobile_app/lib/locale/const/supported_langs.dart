import 'package:flutter/material.dart';

class SupportedLocales {
  static List<Locale> locales = <Locale>[
    Locale('en', 'US'), // We use the FIRST one is the DEFAULT in the code
    Locale('de', 'DE'),
    Locale('hu', 'HU'),
  ];
}
